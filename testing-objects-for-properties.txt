** start of script.js **

function checkObj(obj, checkProp) {
  // Only change code below this line
  let has_property = obj.hasOwnProperty(checkProp);
  if (has_property)
    return obj[checkProp];
  return "Not Found";
  return "Change Me!";
  // Only change code above this line
}

** end of script.js **

